import { groupLookup } from 'event-pusher/helpers/group-lookup';
import { module, test } from 'qunit';

module('Unit | Helper | group lookup');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = groupLookup([42]);
  assert.ok(result);
});
